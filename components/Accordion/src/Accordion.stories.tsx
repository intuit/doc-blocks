import React from "react";

import Accordion from ".";
import notes from "../README.md";

export default {
  title: "Components/Accordion",
  parameters: { notes },
};

export const BasicUsage = () => <Accordion />;
