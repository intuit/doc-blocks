import { Story, Meta, Canvas } from "@storybook/blocks";
import { ThemeProvider, themes, ensure } from "@storybook/theming";

import { RelatedComponents } from ".";
import notes from "../README.md";

<Meta title="Components/Related-Components" parameters={{ notes }} />

# @amalik2/related-components

A component that makes it easy to list a bunch of related components.

<Canvas>
  <Story name="Basic Usage">
    <ThemeProvider theme={ensure(themes.light)}>
      <RelatedComponents
        components={["Components/Responsive Story", "Components/Row"]}
      />
    </ThemeProvider>
  </Story>
</Canvas>
